{
  "$schema": "../gen/schemas/mobile-schema.json",
  "identifier": "mobile-iap",
  "description": "StoreKit tips in the iOS build. Kept out of the default capability so desktop and Android builds, which do not compile the IAP plugin, still validate.",
  "platforms": ["iOS"],
  "windows": ["main"],
  "permissions": ["iap:default"]
}
